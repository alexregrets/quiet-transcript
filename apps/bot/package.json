{
  "name": "@transcriber/bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"Bot tests planned after command handlers grow\"",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@transcriber/core": "workspace:*",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "tsx": "^4.19.4"
  }
}
